# 2025-11-18 KPT

### ✅ Keep (잘한 점, 계속 유지할 것)
- 03:24~04:06 사이 16회 수동 라운드트립 테스트를 수행하여 모든 요청에 정상 응답이 반환됨(사용자 입력: “오늘 테스트 대화입니다.” → 응답: “네, 테스트 대화에 대한 답변입니다.”). 전송-응답 파이프라인이 기본적으로 동작함을 확인했다.
- 테스트 간 간격(수 분 단위)에서 세션 유지와 타임아웃 이슈가 관찰되지 않았고, 동일 입력 반복에도 에러나 중복 처리 문제 없이 일관된 응답이 유지되었다.
- 대화 로그가 시간순으로 확보되어(03:24~04:06) 이후 자동화 테스트의 기준 기대값(고정 응답) 정립에 활용 가능한 근거 데이터를 마련했다.

### ❌ Problem (아쉬운 점, 개선할 점)
- GitHub 커밋이 0건이어서 오늘의 테스트 활동이 코드/테스트/문서로 반영되지 않았다. 재현 가능성과 추적성이 부족하다.
- 테스트 다양성이 부족했다. 단일 문구 반복으로만 검증되어 멀티턴 컨텍스트, 경계값, 오류 경로(빈 입력, 초과 길이, 비정상 포맷 등)는 커버하지 못했다.
- 성능/안정성 지표(지연 p50/p95, 실패율)가 수집되지 않아 SLA 정의나 회귀 감지가 불가능하다.
- 자동화 부재로 수동 테스트에 의존했다. CI에서의 스모크 테스트가 없어 배포 시 회귀를 조기에 차단할 수 없다.
- 운영 모니터링/알림이 없어서 장애나 응답 지연 발생 시 즉시 탐지하기 어렵다.

### 💡 Try (새롭게 시도할 것, 개선 방법)
- 테스트 자산 첫 커밋을 만든다: 라운드트립 스모크 테스트 스크립트와 기대 응답 어서션을 추가한다(예: 커밋 메시지 "test: add chat smoke test for round-trip echo").
- CI 파이프라인을 도입해 PR/메인에서 스모크 테스트를 자동 실행하고 실패 시 머지를 차단한다(예: "ci: add smoke test workflow").
- 요청/응답 메트릭과 구조화 로깅을 추가해 지연(p50/p95)과 성공률을 기록한다. 기본 대시보드를 준비한다(예: "feat/obs: add basic latency metrics & structured logs").
- 테스트 케이스를 확장한다: 다양한 입력 문구, 다중 턴 컨텍스트 유지, 오류 주입(빈 본문, 초과 길이, 비정상 토큰 등)과 회복 시나리오를 포함한다(예: "test: expand chat cases (multi-turn, error paths)").
- 문서화를 보강한다: README에 테스트 실행 방법, 의존성, 기대 결과, 실패 시 트러블슈팅을 정리한다(예: "docs: add chat testing guide").
- 개발 프로세스를 정비한다: 커밋 템플릿과 pre-commit 훅(포맷/린트/테스트)을 설정해 테스트 누락을 방지한다(예: "chore: setup commit template & pre-commit hooks").
- 오늘 대화 로그를 익명화해 테스트 픽스처로 보존하고 회귀 테스트에 포함한다(예: "test: add conversation fixtures (2025-11-18)").